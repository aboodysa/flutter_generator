# CODE_CATALOGUE — what I wrote this session/round, and why

The "what and why" index (AGENTS rule 12). Every artifact written by opencode/claude this
round is listed here: path, what it is, why it exists, status. Additive — never delete.

## Generator fixes (builder/src) — RCA-driven
| Path | What | Why | Status |
|---|---|---|---|
| `builder/src/generators/rule.ts` | Enum comparison fix: `conditionExpr` qualifies enum values as `Priority.high` (was bare `high`); imports only the enum types actually compared in conditions | tasks app surfaced a compile error (`Undefined name 'high'`) for any rule over an enum field; all-import produced `unused_import` warnings | committed `8e262a0` |
| `builder/src/generators/screen.ts` | Parent→children navigation capability: detail screens get `View <Child>s` rows linking to child lists via `<Parent>Id` convention; child list screens filter by `?<fk>=<parentId>` query param | tasks app's follow-ups were unreachable from the task detail screen; general capability for any parent/child sample | committed `c40011e` |
| `builder/src/generators/project.ts` | `generateMain` provides a `BlocProvider` for EVERY distinct screen state (was `screens[0]` only) | multi-entity app crashed with `ProviderNotFoundException` on the second route (RCA-003) | uncommitted → commit with tasks goldens |
| `builder/src/generators/state.ts` + `screen.ts` | **B1: wizard flow-status namespaced as `wizardStatus`** (state.ts: enum-type-based default-qualification in the ctor, `finish()` emits `wizardStatus:`; screen.ts references `state.wizardStatus`) | a wizard step bound to the entity's own `status` field collided with the wizard's internal flow-status field → duplicate_definition analyzer errors; work_auth's review step had a field-less workaround (now restored to bind `status`) | committed `0385e5d` (typecheck clean, validation PASSED, work_auth 22/22) |

## Sample apps (apps/) — per-app artifact convention
| Path | What | Why | Status |
|---|---|---|---|
| `apps/tasks/input/tasks.ir.json` | tasks sample IR: Task (title, dueDate, priority enum, status enum open/closed) + FollowUp (taskId, note, createdAt) + HighPriority rule | demonstrate add/update/delete/close/reopen task + follow-up flows; exercise enum-rule + parent/child capabilities | committed `8e262a0` |
| `apps/tasks/input/rules/HighPriority.oracle.json` | oracle for HighPriority rule (4 cases) | rules without an oracle fail the validator gate | committed `8e262a0` |
| `apps/tasks/output/` | generated app + goldens + rca + validation.txt + README | per-app artifact convention (input left, output right) | committed |
| `apps/tasks/output/rca/RCA-001-enum-rule.md` | RCA for the rule-generator enum bug | root-cause analysis per AGENTS rule ("fix the generator, never the app") | committed `8e262a0` |
| `apps/tasks/output/goldens/*.png` | iPhone-size (390×844) screenshots of task list, task detail, task form, follow-up list | visual evidence for owner (Telegram) | committed `466ee78` |

## Docs (design/flutter-app-builder/)
| Path | What | Why | Status |
|---|---|---|---|
| `CAPABILITIES.md` | General app-type-agnostic capability catalog (L1–L5, MF1–MF6, C1–C2) with 4 sample apps as evidence | plan must be capability-driven, not Ledgerly-driven; each capability validated by ≥2 different app types | committed `739f933` |
| `LEDGERLY_MVP.md` | Ledgerly-MVP milestone: slice map (MVP item → generator slice), sequence, acceptance proof | Ledgerly is ONE sample of the general capability plan | committed `739f933` |
| `CODE_CATALOGUE.md` | this file | the what-and-why index the owner asked for | new |
| `research/PAYMENTS_OPTS.md` | Payments options report: SA/GCC provider landscape (Stripe, moyasar, Tap, HyperPay, PayTabs, Checkout.com, Paymob, wallets/BNPL), a `PaymentGateway` port + `MockPaymentGateway` + DTO + idempotency generator design, adapter-first recommendation (moyasar/Tap/Stripe), PCI posture, and now-vs-backend roadmap | input to a future `payments.v1` generator capability (deterministic, offline-first, SA-first); research-only spike per `~/temp/opencode/flutter-app-builder/RESEARCH_PAYMENTS.md` | new, uncommitted |
| `research/AUTH_OPTS.md` | Auth options report (295 lines): 2026 provider landscape (Supabase, Firebase Auth, Clerk, Keycloak, DIY JWT/OIDC) + biometric/passkeys; maps real auth onto the existing MF2 demo-auth (`builder/src/generators/auth.ts` Session/personas + route `guardPath` + repo `_inScope`/`_stampTenant`); proposes an `AuthProvider` port + `MockAuthProvider` (offline truth, byte-identical output), claims→Session mapper, `secureSession`/`biometric` emission, `[auth]` validator gate, Supabase-first adapter order, and now-vs-P9 roadmap | input to a future `auth.v2` generator capability (deterministic, offline-first, keeps tenant-scoped repos unchanged); research-only spike, web search disabled this run (facts flagged for go-live verify), no code changes | new, uncommitted |
| `research/TASK_SWIFTUI_REQUIREMENTS.md` | SwiftUI-target task brief (owner spec): same IR → native iOS SwiftUI app via a new `attributes.platform`/`targets` knob; additive `builder/src/generators/swiftui/`; no IR fork, no touching Flutter generators; MUST contain 8 numbered sections (tooling ground truth, minimal-diff arch, hard mapping problems, output shape, verification gates, slice plan, risks) | the requirements doc claude will write this slice; this brief is its contract | new, uncommitted |
| `research/SWIFTUI_GROUND_TRUTH.md` | Read-only ground-truth evidence bundle (456 lines, 25 sections) for the SwiftUI target: verbatim Xcode 26.3/Swift 6.2.4/simctl/flutter tooling output, repo+registry+IR surface (`types.ts:140-167` has NO platform key), state/screen/routing/scoring/oracle/validate citations (e.g. `state.ts:281,289,298` wizardStatus, `index.ts:66-81` registry, `oracle.ts` gates), capability stack (money/verdicts/split/auth/budget/export/outbox), rule-portability recommendation (portable evaluator, P9-B6 precedent), SPM-over-pbxproj recommendation, gap analysis (SwiftUI is greenfield), risks + slice groundwork | evidence bundle so SWIFTUI_REQUIREMENTS.md is grounded and tokens-first; inspect-only round: no builder/src edits, no commits | new, uncommitted |
| `research/SWIFTUI_REQUIREMENTS.md` | Requirements + target architecture for the SwiftUI target (the implementation contract for OpenCode). Recovered via CFT/CDP from a ChatGPT share (full doc embedded in the page as a sandbox artifact; double-JSON-unescaped). 1332 lines: goal+non-goals, verbatim tooling (fixed: no `(...)` elisions), two verification gates (Gate A `swift build`/`swift test`; Gate B `xcodebuild -sdk iphonesimulator`), `attributes.platform` as a generation-target scalar with `targets[]` reserved for multi-target, `Package.swift` MUST declare `platforms:[.iOS(.v17)]`, SwiftData as target-only adapter (IR never gains `@Model`), strongly typed generated RuleModel + one-semantics-two-runtimes + mandatory oracle parity, SPM output shape, slice plan S1–S14, risks | review approved-with-corrections; 6 corrections + master principle ("target generators translate, never reinterpret IR semantics") folded in | new, uncommitted |
| `research/S1_IMPLEMENTATION_BRIEF.md` | S1 implementation brief handed to claude (claude-flutter-grill) on 2026-08-16: detailed instructions for the schema/platform knob slice — §2.1 types.ts edit already applied (verify, don't redo), §2.2 constraint via operations.ts `targetOf`/`isSwiftUI` predicate precedent (no application.schema.json exists; 15 per-model schemas untouched), §2.3 validate.ts `[platform]` gate additive-only, §2.4 index.ts target dispatch (swiftui → clear not-yet-implemented failure, exit!=0), §2.5 determinism/header parity, §2.6 clean code + SOLID (owner requirement), §3 hard constraints (no generators/schemas/swiftui-dir/targets[] edits), §4 verification (typecheck, regen 10 IRs twice → byte-identical diff, validator suite, 4 platform probes), §5 report-back format | the implementation contract claude executes; queued + accepted by claude (reading brief, mustering) | new, uncommitted |
| `research/S2_IMPLEMENTATION_BRIEF.md` | S2 implementation brief handed to claude (claude-flutter-grill) 2026-08-16 after S1 landed: SwiftUI module skeleton / hello screen — §2.1 new `builder/src/generators/swiftui/` module (pure `(IR,ctx)→string`: `Package.swift` MUST declare `platforms:[.iOS(.v17)]` correction 3, `@main` SwiftUI App + hello View, one XCTest, barrel `generateSwiftUITarget` returning `{files:[]}`), §2.2 index.ts: replace the S1 swiftui throw with real emission into `<out>/ios/` (additive, Flutter path byte-identical, `GenerateResult` preserved, deterministic, `[generated]` headers), §2.3 validate.ts additive gates `[swiftpkg]`/`[swiftarch]`/`[swiftdeterminism]` (isSwiftUI-guarded), §2.4 Gate A `swift build`+`swift test` and Gate B `xcodebuild -sdk iphonesimulator` pinned to iPhone 17 Pro OS 26.0 (§6.4, correction 2), §3 hard constraints (no Flutter generator/schema edits, no swiftui-flutter dir, no targets[], no @Model/SwiftData, no CRUD/rules — those are S3+), §4 verification incl. probe IR at `apps/tasks/output/qa/s2_probe.ir.json`, §5 report-back format | S1 approved+verified (typecheck OK, byte-identical, 20 gates PASS incl `[platform]`); S2 landed + verified, then **PARKED/DEFERRED per owner directive** (commit `ca0eb39`), main capability loop resumed | committed `ca0eb39` |
| `research/S1_IMPLEMENTATION_BRIEF.md` | (see row above for S1+S2 status) | verified S1 + S2 committed together `ca0eb39` | committed `ca0eb39` |
| `research/MOBILE_UI_PATTERNS.md` | Read-only mobile design interface-pattern spike (owner request): 2026 platform landscape (Material 3 Expressive / I/O 2026 nav compaction model — bottom nav 3-5 destinations, rail on medium, drawer deprecated; iOS 26 Liquid Glass + WWDC26 search patterns), ground truth that generated apps have NO global shell/search/adaptive behavior, ranked codegen pattern catalog P1-P5 (P1 global NavigationBar shell for multi-feature apps — closes ledgerly LM6/LM7 "no entry point" class; P2 searchable list SearchBar+filter; P3 on-scroll app-bar/nav-bar; P4 extended FAB + detail "…" menu for export/audit; P5 composed empty/error/loading), 2026 trap-avoidance (no drawer/"More" overflow, shell caps at 5 features, no search-tab in V1, no glass emulation on Flutter, no third-party pattern libs), priority roadmap + acceptance gate (CDP-verified, goldens) | research spike only — no builder/src changes, no commits; feeds the S0 interface-pattern contract | new, uncommitted |
| `research/INTERFACE_PATTERN_CONTRACT.md` | **S0 interface-pattern contract** (owner-reviewed 2026-08-16; the implementation-ready requirement that supersedes the spike): deterministic pattern selection master principle (patterns chosen from IR semantics + composition context, never hidden heuristics in screen generators; one selection owner = composition.ts + plan payload); locked decisions P1 global shell (first, destination contract: stable feature id + title + primary/root screen + deterministic icon map, features[] order = destination order, indexedStack per-destination state, **>5 = target-capability planning error, NOT an IR validation error**), P2 per-list search (second, explicit `search:` composition block with enabled predicate + field + mode instead of screen.ts deciding), P3 scroll cosmetic presentation-only (IR state ≠ scroll state), P4 capability-driven actions (export→Export, delete→Delete, audit→Audit, write→Save; never "always show …"), **P5 merged into DESIGN_OPTS D2 (no duplication; this contract only defines structural placement)**; frozen roadmap S0→P1→P2→P3→P4→P5/D2; acceptance invariant same IR+same ctx+same generator version → byte-identical | owner review folded in verbatim; P1/P2 are the next interface investments addressing the structural gaps not CRUD polish | new, uncommitted |
| `research/P1_IMPLEMENTATION_BRIEF.md` | P1 implementation brief handed to claude (claude-flutter-grill) 2026-08-17 after main loop COMPLETE: global navigation shell — multi-feature only (ledgerly 4 features; tasks/work_auth/hr_service single, BYTE-IDENTICAL), `StatefulShellRoute.indexedStack` + `NavigationBar` from `features[]` order, centralized composition selector (`shellFor(features)` + plan.json `patterns.shell`), destination contract (stable id + humanized primary-entity title + root route + fixed icon stem map), **>5 features = explicit generation-time target-capability error (not IR/schema error)**, `[shell]` validate gate additive, single-feature byte-identical proof + 6-feature probe + CDP drive (per-tab stack, overflow 320-1280) under `apps/ledgerly/output/qa/p1-shell/` | accepted by claude, in progress (reading plan.ts) | new, uncommitted |
| `research/GRILL_S0_REPLY.md` | **Grill of the S0 ChatGPT reply** (run on remote opencode, tracematrix `germany3`, deepseek-v4-flash-free, 2026-08-17): 16 numbered challenges to the interface-pattern contract. Top hits: C1 "ctx" undefined → determinism invariant is a tautology (ctx presumably LLM-authored like IR); C2 N=0/1/4 features & hard-abort-vs-fallback for >5 unspecified; C3 co-owned generators break "untouched unless slice owns them"; C4 P2 "only when title/name field exists" is the exact banned name-based heuristic; C5 single-field presumption + mode×type matrix missing; C6 "server-query without touching screen generators" unverified (async/pagination = new architecture); C7 P3 cosmetic collides with pagination/scroll-restoration; C8 list-vs-grid selection has no deterministic home; C9 capability→action static 1:1 — enablement/confirm/params/overflow missing; C10 build-time capability vs runtime authorization conflated; C11 P5-into-D2 creates two placement owners + runtime errors not in IR; C12 byte-identical broken by toolchain hermeticity (versions/dates/formatter); C13 icon collisions/missing glyphs; C14 deep links + per-tab restoration implied by "stable id" but undelivered; C15 LLM-authored plan recurses nondeterminism; C16 unknown capabilities need closed vocabulary + error policy | remote grill per owner "use my servers opencode to grill reply"; sent to owner Telegram msg 844 (doc) + 845 (summary) | new, uncommitted |
| `research/SPIKE_PROTOCOL.md` | **Spike operating protocol** (owner-provided ChatGPT insight 2026-08-17): the binding execution rules for EVERY spike (local claude + remote opencode). Spikes are research → decision (`ADOPT`/`MODIFY`/`REJECT`/`DEFER`/`SPLIT`/`ESCALATE`), never assumed-solution implementation. 20 non-negotiable rules: read-only-by-default, no commits during research, inspect repo source AND generated output, prefer IR semantics over naming heuristics, deterministic home for every decision, ownership/insertion-point rule (never fork a generator), failure-mode outcomes mandatory, build-time capability ≠ runtime auth, presentation state ≠ data-flow, explicit §17 report structure, 18 per-spike expectations (S-CTX/P3/P4/P5/ M4/S-HERMETIC/S-DEEPLINK), "investigate first. prove second. decide third. implement last."; referenced from AGENTS.md non-negotiables #5 + semantic-lane section | owner directive: write principles + reference from AGENTS.md; binding for all agents | committed 5bbc616+ |

## P2 — per-list search (builder/src) — 2026-08-17
| Path | What | Why | Status |
|---|---|---|---|
| `builder/src/types.ts`, `builder/schemas/entity.schema.json` | New optional `EntityModel.primaryDisplayField?: string` — the entity's own declared primary human-readable identifier, must reference an existing `String`-typed field on that entity | P2's search predicate must be semantic (grill C4), never a `title`/`name`/`label` name-guess — this is the one explicit IR key the selector is allowed to read; additive, absent = no search, byte-identical | committed `99da57b` |
| `builder/src/composition.ts` | `SearchSpec`, `searchFor(screen, entity, repo)`, `searchTargets(ir)` — the P2 pattern selector (same centralized-decision posture as P1's `shellFor`); single field, mode `contains` only, case-insensitive | contract §4 + grill C4/C5/C6 resolutions locked into the brief: semantic trigger, single-field scope, in-memory-only mode | committed `99da57b` |
| `builder/src/gen_context.ts` | `GenContext.search?: Map<screenName, SearchSpec>` | screen.ts consumes the decided payload by screen name, never re-derives | committed `99da57b` |
| `builder/src/generators/screen.ts` | List-screen template renders `SearchBar` + filter-as-you-type + no-results `EmptyState` ONLY when `ctx.search` has an entry for that screen (bloc-only this slice, same documented gap L3's export buttons already carry); converts to `StatefulWidget` only when search is enabled (local `_query` state lives outside the Cubit) | contract §4: search is UI-payload-driven, not a screen.ts heuristic; P3's "IR state ≠ UI state" principle applied the same way | committed `99da57b` |
| `builder/src/plan.ts`, `builder/src/index.ts` | `GenerationPlan.patterns.search?: Record<screenPath, SearchSpec>` (additive alongside `patterns.shell`, `shell` now optional within `patterns`); `searchByPath()` re-keys the name-keyed selector map into the path-keyed plan.json shape the brief specifies | "decision as data," same posture P1's `patterns.shell` already established; keyed by screen path per the brief's explicit instruction | committed `99da57b` |
| `builder/src/validate.ts` | New `[search]` gate: re-derives `searchTargets` fresh from the raw IR and diffs against `plan.json`'s recorded decision (path-for-path), then cross-checks the generated screen files — only marked screens render `SearchBar`, none of the others do | deliverable 4 — re-assert the selector + prove the artifact matches, same posture `[fidelity]`/`[shell]` already take | committed `99da57b` |
| `builder/src/generators/test.ts` | `generateScrollTest` (bloc + riverpod) switched from a single fixed-magnitude `tester.drag(...)` to Flutter's own `tester.scrollUntilVisible(finder, delta, scrollable: ...)` SDK helper, with an explicit `scrollable:` finder scoped to the ListView's own `Scrollable` (a searchable screen now has two — the list's and the SearchBar's internal text field's) | real regression found via CDP walk: a single fixed drag no longer reliably reached the last seeded row once a screen's own viewport shrank (SearchBar added above the list) — root-caused via bisection (`ScrollableState.position.jumpTo` DID reach it, proving the drag-and-settle gesture simulation was the fragile part, not search's rendering); the fix is strictly more robust regardless of search | committed `99da57b` |
| `apps/{tasks,hr_service,work_auth,ledgerly}/input/*.ir.json` | `primaryDisplayField` declared on every list-screen entity with a natural single free-text identifier: Task→title, FollowUp→subject, LeaveRequest→name, Approval(hr)→approver, WorkAuth→name, VisaQuota→name, User/ExpenseClaim/Approval(ledgerly)/MealBudget→name | proves the capability for real (LM6-style IR authoring, not just generator plumbing); Approval validated across 2 different app types (hr_service + ledgerly), matching the CAPABILITIES.md "≥2 samples" convention | committed `dafe4b1` (all 4 apps regenerated + goldens; `validate.ts` 22/22 gates PASS incl. `[search]`) |
| `apps/ledgerly/output/qa/p2-search/`, `apps/tasks/output/qa/p2-search/` | CDP walk notes (`P2-search-cdp-walk.md`) + the four-breakpoint overflow probe (`overflow_probe_test.dart`, kept as a QA artifact — copied into the real `test/` dir to run, removed after) | deliverable 6: filter/no-results/clear verified live in-browser on both apps; browser `resize_window` non-functional in this session (same limitation as the P1 walk) — substituted a directly-controlled Flutter widget-test viewport as an equally rigorous 320/390/768/1280 proof | committed `f48e5a6` — **P2 CLOSED** |

## SPIKE M4 — sealed-class state codegen (research-only) — 2026-08-17
| Path | What | Why | Status |
|---|---|---|---|
| `research/SPIKE_M4_REPORT.md` | Full spike report (research-only, per `SPIKE_PROTOCOL.md`, run on remote opencode tracematrix `germany3`): investigates `LEFTOVER_NOTES.md` M4 (scoring.ts can select `"sealed-events"` but `state.ts` never implements it). Proves `SPIKE_PLAN.md`'s prior ground truth wrong (rasheed's `AllExpenses` state DOES cross the threshold; the already-shipped `[strategy-fidelity]` gate already catches the resulting mismatch); root-causes the real defect as `scoreStateStrategy`'s metric (status/field count) contradicting DESIGN §5.2's stated `stateMachines` transition-surface metric; H1 (implement sealed now) fails the evidence test (reference app is enum-status-dominant, no IR declares any `stateMachines`). Decision: **MODIFY** — M4a corrects the selector (scoring.ts-only, S), M4b (sealed template family) explicitly deferred until a real event-rich IR exists. 17 numbered sections incl. rejected alternatives (§15) and follow-up (§17) | closes the open M4 leftover-note item with an evidence-backed decision instead of either silently implementing the wrong thing or leaving it open indefinitely | committed `b5eb50c` |
| `apps/rasheed/output/qa/m4_evidence.ts` | Evidence harness: runs `scoreStateStrategy` over all 15 repo sample/app IRs under both the current metric and the DESIGN-§5.2-stated `stateMachines` metric, printing a comparison table | reproducible proof that only rasheed's `AllExpenses` fires sealed today, and that zero IRs have any `stateMachines` surface — the concrete evidence behind the MODIFY decision | committed `b5eb50c` |
| `apps/rasheed/output/qa/probe1/` | Probe generation of `builder/samples/rasheed.ir.json` (full generated app: lib/, test/, web/, pubspec.yaml, plan.json, builder.lock.json, regions.json) | proves the plan/emit mismatch for real — `plan.json` declares `state:AllExpenses \| strategy=sealed-events`, emitted `all_expenses.dart` header is `template=state_enum_status.v1` | committed `b5eb50c` |
| `apps/rasheed/output/qa/validate_probe1.log` | Full `validate.ts` output on `probe1` | proves `[strategy-fidelity] FAIL (1)` / `VALIDATION FAILED` — the negative-control evidence that the drift gate already exists and already fires; all other 21 gates PASS on the same probe | committed `b5eb50c`; **refreshed for M4a (PASS)** |

## M4a — corrected `scoreStateStrategy` selector (2026-08-17)
| Path | What | Why | Status |
|---|---|---|---|
| `builder/src/scoring.ts` | `scoreStateStrategy(s, ir)` now decides `sealed-events` purely from declared IR semantics: a `stateMachines` entry whose state vocabulary matches the state's declared `statuses` AND declares `events`+`transitions`. Removed `SEALED_EVENTS_THRESHOLD=8` and the synthetic `["initial","loading","success","failure"]` status list (owner directive: no hardcoded magic numbers). All other callers default to `enum-status` | fixes the M4 defect — before, rasheed's `AllExpenses` (5 statuses + 6 extraFields = 11) selected sealed-events while the generator only ever emits enum-status, so `[strategy-fidelity]` FAILed | committed this round |
| `builder/src/arch.ts` | call site passes `ir` through to `scoreStateStrategy(s, ir)` | wiring the IR into the selector so it can read the declared `stateMachines` surface | committed this round |
| `builder/src/index.ts` | dropped the now-unused `scoreStateStrategy` import | dead-import cleanup (M4a touched the signature) | committed this round |
| `apps/rasheed/output/qa/m4_evidence.ts` | updated to the fixed signature + fixed metric: prints the per-IR strategy table via `scoreStateStrategy(s, ir)` + matching-machine count | reproducible proof that sealed-events now fires nowhere (no IR declares a matching machine), honest with the generator | committed this round |
| `apps/rasheed/output/qa/probe1/plan.json` | `state:AllExpenses` strategy flipped `sealed-events` → `enum-status` (lib/ byte-identical) | the plan now describes the code the generator actually emits | committed this round |
| `apps/rasheed/output/qa/validate_probe1.log` | refreshed `validate.ts` output on probe1: `[strategy-fidelity] PASS` (was FAIL) | M4a gate proof | committed this round |
| `design/flutter-app-builder/research/SPIKE_PLAN.md` | M4a acceptance checkboxes marked `[x]` | records M4a completion against the frozen plan's acceptance gates | committed this round |

## Temp/QA harnesses
| Path | What | Why | Status |
|---|---|---|---|
| `apps/tasks/output/app/test/temp_all_screens_golden_test.dart` | temporary golden harness rendering all screens (detail/form/follow-up list) through the real router | the generated golden test only renders list screens; this captures every screen as visual evidence; **keep as a reference for a future "golden all screens" generator capability** | present in working tree, NOT committed (regenerated out) — recreated on demand |
| `apps/tasks/output/rca/RCA-003-multi-cubit-main.md` | RCA for the multi-cubit main.dart crash | root-cause analysis of the real app bug found by the harness | committed `275da9d` |
| `apps/tasks/output/app/web/` | web platform for the tasks app (`flutter create . --platforms web`) + release build at `--base-href=/tasks/` | prerequisite for Tailscale expose (AGENTS rule 13) | uncommitted → commit |
| `apps/tasks/output/qa/tailscale_static_server.js` | node SPA-fallback static server serving `build/web` on 127.0.0.1:8081 | serves the tasks app through Tailscale Serve at `/tasks` without touching the mall app's `/` + `/api` mounts | running (nohup); copy under `apps/tasks/output/qa/` |
| `../AGENTS.md` (repo root) | Added **"Remote opencode hosts"** section per owner request 2026-08-17: drive opencode over SSH on `tracematrix` (`ssh root@tracematrix.businessanalystcrew.org`, opencode v1.17.15, tmux channels `germany`/`germany3`) + `tracematrix001` (`/root/.opencode/bin/opencode`, `bp-claude`/`bp-watch`/`ooo`) via tmux; attach/reattach pattern, send-keys + capture-pane control, persist-across-disconnect, remote-work rules (read host CLAUDE.md first, small commits, no heavy builds on 1vcpu boxes, confirm idle before typing, park non-paying plan dialog) | owner asked to "use opencode in tracematrix/tracematrix001 using ssh root@… and tmux channel, write instructions in agents md file"; channel `germany3` verified live + idle (opencode running in `/flutter_generator`) | new, uncommitted |

## S-CTX decision brief + mermaid diagrams (2026-08-17)
| Path | What | Why | Status |
|---|---|---|---|
| `research/SCTX_DECISION.md` | Owner decision brief for the S-CTX slice in the owner-suggested format (exec summary → context → why/impact → options with priority+impact → findings → decision), incl. mermaid pipeline + sequence diagrams with shaded areas of interest | lets the owner make an informed go/no-go on S-CTX before implementation; diagrams make the gate/plan/DR provenance visible | new, uncommitted (MD+PDF both to be sent) |
| `research/SCTX_DECISION.pdf` | Same brief rendered to PDF (Chrome `--headless --print-to-pdf`) with the mermaid diagrams embedded as PNGs | PDF is the deliverable the owner reads on the phone; markdown can't render mermaid | new, uncommitted |
| `research/mermaid/plan_pipeline.mmd` | Mermaid flowchart: IR → selectors → plan.json → generators → validate gates; amber = new `[plan-determinism]` gate, blue = decision-as-data, dashed = gate grouping | reusable diagram source (rule 12: sources kept in repo; PDF depends on re-rendering them) | new, uncommitted |
| `research/mermaid/plan_sequence.mmd` | Mermaid sequence: approve → selectors write plan → validate re-derives + diffs (alt: drift → FAIL); blue/amber rects = S-CTX areas of interest | same as above | new, uncommitted |
| `research/mermaid/puppeteer-config.json` | Pins puppeteer to CFT chrome (`chrome-mac-arm64/Google Chrome for Testing`) for `mmdc`| plain `mmdc` fails with "Could not find chrome-headless-shell" — the config makes diagram rendering reproducible on this mac | new, uncommitted |

## S-CTX — plan-determinism contract + gate (2026-08-17)
| Path | What | Why | Status |
|---|---|---|---|
| `research/DETERMINISM_CONTRACT.md` | Field-by-field derivation map of `GenContext` + `GenerationPlan` (scoring / patterns.shell / patterns.search / entry.*), each pure selector cited (file:line), + the transitive-purity invariant | resolves grills C1 + C15 (S-CTX): "the plan is IR-derived" becomes a written, cited contract instead of a tautology | committed this round |
| `builder/src/validate.ts` | new `[plan-determinism]` gate (in `validateOutput`): reuses the existing `[determinism]` fresh regen, JSON-compares the regenerated `plan.json` vs the on-disk one; new `planDeterminism` field in `ValidationResult` + `main` printout | standing guard: catches hand-edits / stale plan / an LLM-or-purity leak in any plan-field helper (C15); additive, zero generator changes | committed this round |
| `research/SCTX_DECISION.md` + `.pdf` + `research/mermaid/*` | owner decision brief (exec summary/context/why/options with priority+impact/findings/decision) + mermaid pipeline/sequence diagrams with shaded areas of interest | informed go/no-go on S-CTX before implementation; delivered to owner on Telegram as attachments | committed `5a67f5f` |

## P3 — per-screen on-scroll AppBar tint (2026-08-17)
| Path | What | Why | Status |
|---|---|---|---|
| `builder/src/composition.ts` | `ScrollSpec {enabled:true}` + `scrollFor(screen)` (declared contract rule: `scroll.enabled = screen.kind ∈ {list, detail}`, INTERFACE_PATTERN_CONTRACT §5, ChatGPT round-2 edit #2) + `scrollTargets(ir)` (name-keyed, same shape as `searchTargets`) | the ONE place that decides scroll; screen.ts only renders the decided payload (contract §1 master principle). State-management AGNOSTIC on purpose (unlike P2 search's bloc-only carve-out) so the riverpod sample tints too — no latent [scroll] gate gap | committed `f0254f9` |
| `builder/src/gen_context.ts`, `builder/src/plan.ts` | `GenContext.scroll?: Map<screenName, ScrollSpec>`; `GenerationPlan.patterns.scroll?: Record<screenPath, ScrollSpec>` (additive alongside shell/search) | "decision as data" — same posture P1/P2 established | committed `f0254f9` |
| `builder/src/index.ts` | `scrollByPath()` re-keys the selector map into path-keyed plan.json; `writePlan` gained 9th `scroll?` param; threaded through single- and multi-feature paths | wires the decision into ctx + plan deterministically | committed `f0254f9` |
| `builder/src/generators/screen.ts` | `scrollEnabled = !!ctx?.scroll?.get(s.name)`; `NotificationListener<ScrollNotification>` wrapper + widget-local `_scrolled`; AppBar `backgroundColor: _scrolled ? surfaceContainerHighest : null` (null at rest = theme default ⇒ byte-identical goldens; surfaceContainerHighest = stock M3 token, passes [architecture] raw-color gate); riverpod list/detail → ConsumerStatefulWidget, bloc → StatefulWidget only when `needsLocalState` (scroll||search); template tag gains `_scroll` suffix | renders the scroll behavior; `_scrolled` is widget-local UI state (contract §5 "IR state ≠ scroll/UI state") | committed `f0254f9` |
| `builder/src/validate.ts` | `[scroll]` gate + exported `scrollCheck`: re-derives via the SAME `scrollTargets`, cross-checks plan.json patterns.scroll (missing/wrong/stale), scans EVERY screen — list/detail must render the listener, wizard/form must NOT | standing guard proving both the positive set (tints) AND the null set (wizards stay clean) | committed `f0254f9` |
| `apps/tasks/output/qa/p3-scroll/scroll_negative_harness.ts` | output-side negative control: fresh generate to temp dir, calls exported `scrollCheck`, strips the listener → `[scroll] FAIL(1)` on control-opposite | proves the gate is non-vacuous (catches listener-strip; the plan-side stale-entry negative control is proven in PROBE_FINDINGS) | committed `f0254f9` |
| `apps/tasks/output/qa/p3-scroll/PROBE_FINDINGS.md` + `cdp/*.png` | CDP walk evidence (tint fires on list & detail, reverts at top, no overflow at 320/390/768/1280) + pre-existing golden-harness GetIt bug note | UI-slice CDP gate evidence (AGENTS rule 15); root-cause note that the 5 temp-harness failures are pre-existing (P1 harness re-registers GetIt per test), not P3 | committed `f0254f9` |
| `research/P3_DECISION.md` + `.pdf` | owner decision brief (exec summary/context/why/options with priority+impact/findings/decision) + mermaid pipeline/sequence diagrams with shaded areas (amber = scrollFor + [scroll] gate, blue = decision-as-data) | informed go/no-go on P3; delivered to owner on Telegram as attachments | committed `f0254f9` |

## Post-submit back button fix (RCA-007) (2026-08-17)
| Path | What | Why | Status |
|---|---|---|---|
| `builder/src/generators/crud_form.ts` | Post-submit navigation is now target-aware (`postSubmitNav`): detail path → `context.pushReplacement('/entity/:id')` (replaces the form, keeps the list beneath it → AppBar auto-back renders); list path → keep `context.go(listPath)` (home has no parent to pop to) | owner-reported: after create there was no in-app back button (browser back only, absent on iPhone). `context.go` collapses the go_router stack, so the detail screen was the sole entry (`canPop=false` → no auto-back). Form is always `push`ed from the list, so the list sits beneath it | committed `ac3939d` |
| `apps/tasks/output/rca/RCA-007-post-submit-back-button.md` | Full RCA (symptom/investigation/root cause/fix+rejected alternatives/rationale/verification/prevention) | root-cause analysis per AGENTS "fix the generator, never the app" | committed `ac3939d` |
| `apps/tasks/output/qa/nav-back/*.png` | CDP evidence: form (Back), post-create detail (Back now present), post-edit detail (Back), back → list with new item | UI-slice CDP gate evidence (AGENTS rule 15) | committed `ac3939d` |
| `LEFTOVER_NOTES.md` (proposed) | optional `[nav]` validate gate asserting form submits use `pushReplacement`/`pop` (never bare `go` to a detail path) | harden the same [scroll]/[search] re-derive-and-diff posture for navigation | not done this slice — tracked |

## P4 — capability-driven actions (2026-08-17)
| Path | What | Why | Status |
|---|---|---|---|
| `builder/src/composition.ts` | `ActionSpec`/`ActionKind`/`ActionPresentation` + `ACTION_ICONS`/`KNOWN_ACTION_ICONS` + `actionsFor(screen,entity,repo,ir)` (single action decision; edit/export/delete/audit/save; >2 detail → non-edit overflow) + `actionsTargets(ir)` | owner-accepted S-P4 MODIFY; contract §6 | committed `a8629f6` |
| `builder/src/generators/screen.ts` | consumes `ctx.actions` by name; Delete confirm dialog (`showDialog`, Cancel=no-mutation); Audit `/audit-log`; overflow `PopupMenuButton` dispatch-by-kind; export via exportButtons; save semantic-only | renders the decided payload, never re-derives/counts (S-P4 invariants 1/3) | committed `a8629f6` |
| `builder/src/generators/infra.ts` | locale vocab += `cancel`, `audit` (both maps + getters) | P4 chrome labels localizeable (L4 fixed-vocab) | committed `a8629f6` |
| `builder/src/generators/test.ts` | crud flow test taps confirm dialog; opens overflow menu when actionsFor overflowed Delete (uses actionsTargets) | regression test matches new UI | committed `a8629f6` |
| `builder/src/validate.ts` | `export function actionsCheck` + `[actions]` gate (re-derive via same actionsTargets, diff plan, scan screens, delete-confirm + no-save-FAB + closed vocab checks) | neg-controls proven: stale plan / delete-strip / save-FAB all caught | committed `a8629f6` |
| `builder/src/{plan.ts, gen_context.ts, index.ts}` | `patterns.actions`, `ctx.actions`, `actionsByPath`, writePlan 10th param | decision-as-data wiring | committed `a8629f6` |
| `apps/hr_service/output/qa/p4-audit-nav_test.dart` + `p4-actions/*.png` | widget test (audit nav via Flutter test framework) + CDP screenshots (overflow menu, delete-confirm, audit) | deterministic UI-slice verification (CDP popup-menu geometry is stale in web) | committed `d289f59` |
| `apps/{tasks,ledgerly,work_auth}/output/...` screen+plan+crud_flow_test | regenerated app outputs with P4 action UI + updated flow tests | per-app evidence | committed `a8629f6` |

## Model-tier implementer separation (AGENTS.md, 2026-08-17)
| Path | What | Why | Status |
|---|---|---|---|
| `AGENTS.md` | New "Model tier — implementer separation" section: zen model = orchestrator only, never edits impl files; delegates code to remote opencode agents (tracematrix/tracematrix001) via tmux; cross-ref'd from remote-rules | owner directive: don't implement with zen models, manage only | committed `a2b3c14` |
